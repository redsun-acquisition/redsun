# Migration

One page per release that changes existing code. Each lists what changed and
what to write instead. The [changelog](../reference/changelog.md) lists every
change; these pages show how to move code across the ones that break it.

- **[From 0.13 to the next release](0.13-to-next.md)**: the container layer
  is replaced by sessions.
