devices:
  mock-stage: mock_bundle.devices:MockStage

presenters:
  mock-motor: mock_bundle.presenters:MockMotorPresenter
  mock-late: mock_bundle.presenters:MockLatePresenter

views:
  mock-motor-view: mock_bundle.views:MockMotorView

providers:
  mock-services: mock_bundle.services:MockServices
