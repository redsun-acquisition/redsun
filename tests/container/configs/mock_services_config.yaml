schema_version: 1.0
frontend: pyqt
name: mock-services-session
services:
  launched:
    plugin_name: mock-pkg
    plugin_id: stand_in
    prefix: "SIM:"
    stop_timeout: 2
  beamline:
    prefix: "BL01:"
devices:
  motor:
    plugin_name: mock-pkg
    plugin_id: my_motor
